# Normaliza fim de linha: LF no repositório, nativo no disco.
# Sem isso, o Windows gera diffs de arquivo inteiro só por CRLF.
* text=auto eol=lf

# Binários, se aparecerem
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
